# create a folder => /app/components/bootstrapJsPkg.jsx => 
------------------------------------------------------------
'use client'
import React, { useEffect } from 'react'

const Bootstratp5 = () => {
    useEffect(() => {
        import('bootstrap/dist/js/bootstrap.bundle.min.js');
    }, []);
  return null;
}

export default Bootstratp5


# goto layout /app/layout => 
----------------------------
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

// Importing Bootstrap CSS and JS
import 'bootstrap/dist/css/bootstrap.min.css';
import Bootstrap5 from "../components/bootstrap5";



export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}

        <Bootstrap5 />  // *** import the bs5 js pkg for animation .
      </body>
    </html>
  );
}
