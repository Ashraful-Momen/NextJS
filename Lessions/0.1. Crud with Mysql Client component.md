Awesome — here is **Next.js CRUD with MySQL** using Prisma ORM.
We’ll build full working CRUD with a real DB.

---

## Project Structure (visual)

---

# 1️⃣ Install packages

```bash
npm install prisma @prisma/client mysql2
npx prisma init
```

---

# 2️⃣ Configure MySQL connection

Open `.env`

```env
DATABASE_URL="mysql://root:password@localhost:3306/mydb"
```

Replace:

* `root` = your mysql user
* `password` = your mysql password
* `mydb` = database name

Create DB if not exists:

```sql
CREATE DATABASE mydb;
```

---

# 3️⃣ Prisma schema

**prisma/schema.prisma**

```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model User {
  id        Int      @id @default(autoincrement())
  name      String
  createdAt DateTime @default(now())
}
```

Apply migration:

```bash
npx prisma migrate dev --name init
```

This creates `User` table in MySQL.

---

# 4️⃣ Create API Routes

## ➤ GET + POST

**app/api/users/route.js**

```js
import { PrismaClient } from "@prisma/client";
const prisma = new PrismaClient();

export async function GET() {
  const users = await prisma.user.findMany();
  return Response.json(users);
}

export async function POST(req) {
  const { name } = await req.json();
  const user = await prisma.user.create({
    data: { name },
  });
  return Response.json(user);
}
```

---

## ➤ DELETE user

**app/api/users/[id]/route.js**

```js
import { PrismaClient } from "@prisma/client";
const prisma = new PrismaClient();

export async function DELETE(req, { params }) {
  const id = parseInt(params.id);
  await prisma.user.delete({ where: { id } });
  return Response.json({ success: true });
}
```

---

# 5️⃣ Frontend – Client CRUD page

**app/users/page.jsx**

```jsx
"use client";
import { useEffect, useState } from "react";

export default function UsersPage() {
  const [users, setUsers] = useState([]);
  const [name, setName] = useState("");

  useEffect(() => {
    load();
  }, []);

  const load = async () => {
    const res = await fetch("/api/users");
    setUsers(await res.json());
  };

  const addUser = async () => {
    await fetch("/api/users", {
      method: "POST",
      body: JSON.stringify({ name }),
    });
    setName("");
    load();
  };

  const deleteUser = async (id) => {
    await fetch(`/api/users/${id}`, { method: "DELETE" });
    load();
  };

  return (
    <div>
      <h1>Next.js + MySQL CRUD</h1>

      <input
        value={name}
        onChange={e => setName(e.target.value)}
        placeholder="Enter name"
      />
      <button onClick={addUser}>Add User</button>

      <ul>
        {users.map(u => (
          <li key={u.id}>
            {u.name} {" "}
            <button onClick={() => deleteUser(u.id)}>X</button>
          </li>
        ))}
      </ul>
    </div>
  );
}
```

---

## UI preview

---

# 6️⃣ Run Project

```bash
npm run dev
```

Visit:
👉 `http://localhost:3000/users`

---

# 7️⃣ What you get

✔ Real MySQL database CRUD
✔ Persistent data (not in memory)
✔ Works in production
✔ Clean ORM (Prisma)
✔ Fully working API + UI

---

# 8️⃣ NEXT LEVEL (tell me if you want)

I can provide:

✅ UPDATE user (edit)
✅ Advanced UI (Tailwind + DaisyUI)
✅ Pagination
✅ Search
✅ Sorting
✅ MySQL + Prisma + Docker
✅ MySQL in Docker with docker-compose
✅ Auth with JWT or NextAuth
✅ Role-based access

Just tell me what the next step is — I’ve got you 😊
