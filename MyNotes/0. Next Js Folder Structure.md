Here’s a clean **Next.js (App Router)** folder structure in ASCII format (modern Next.js 13+ / 14+ style):

```
my-nextjs-app/
├── public/                     # Static assets (images, fonts, etc.)
│   ├── images/
│   └── favicon.ico
│
├── src/                        # (optional but recommended)
│   ├── app/                    # App Router (core of Next.js)
│   │   ├── layout.js           # Root layout
│   │   ├── page.js             # Home page (/)
│   │   ├── globals.css         # Global styles
│   │   │
│   │   ├── about/              # Route: /about
│   │   │   └── page.js
│   │   │
│   │   ├── blog/               # Route: /blog
│   │   │   ├── page.js         # Blog list
│   │   │   └── [slug]/         # Dynamic route
│   │   │       └── page.js     # Blog post
│   │   │
│   │   ├── api/                # API routes
│   │   │   └── hello/
│   │   │       └── route.js
│   │   │
│   │   ├── loading.js          # Loading UI
│   │   ├── error.js            # Error UI
│   │   └── not-found.js        # 404 page
│   │
│   ├── components/             # Reusable UI components
│   │   ├── ui/
│   │   │   ├── Button.js
│   │   │   └── Card.js
│   │   └── layout/
│   │       ├── Navbar.js
│   │       └── Footer.js
│   │
│   ├── lib/                    # Utilities, helpers, configs
│   │   ├── utils.js
│   │   └── api.js
│   │
│   ├── hooks/                  # Custom React hooks
│   │   └── useAuth.js
│   │
│   ├── services/               # API/service layer
│   │   └── userService.js
│   │
│   ├── styles/                 # Additional styles (if needed)
│   │   └── variables.css
│   │
│   └── types/                  # TypeScript types (if using TS)
│       └── index.d.ts
│
├── .env.local                  # Environment variables
├── .gitignore
├── next.config.js              # Next.js config
├── package.json
├── README.md
└── tsconfig.json               # If using TypeScript
```

---

### Notes

* `app/` = **new routing system (recommended)**
* `pages/` = **old router (avoid for new projects)**
* `src/` = optional but keeps things cleaner in larger apps
* API routes now use `route.js` instead of `pages/api`

---

