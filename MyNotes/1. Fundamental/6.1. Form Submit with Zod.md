# Next.js Complete Form Validation Note (React Hook Form + Zod)

# 1. Modern Form Architecture

```txt id="a7m2qx"
                NEXT.JS MODERN FORM FLOW
┌──────────────────────────────────────────────────────┐
│                                                      │
│              CLIENT COMPONENT (CSR)                  │
│                                                      │
│   ┌──────────────────────────────────────────────┐   │
│   │         React Hook Form + Zod               │   │
│   │                                              │   │
│   │  - Input Register                            │   │
│   │  - Form Validation                           │   │
│   │  - Error Handling                            │   │
│   │  - Submit Handling                           │   │
│   │  - Loading State                             │   │
│   └──────────────────────────────────────────────┘   │
│                                                      │
└──────────────────────────────────────────────────────┘
                        │
                        ▼
                 fetch("/api/register")
                        │
                        ▼
┌──────────────────────────────────────────────────────┐
│                    API ROUTE                        │
│                                                      │
│   - Validate Again with Zod                         │
│   - Save Database                                   │
│   - Return Error / Success                          │
└──────────────────────────────────────────────────────┘
                        │
                        ▼
                 Prisma ORM + Database
```

---

# 2. What is React Hook Form ?

React Hook Form is a form library for React.

It helps with:

* Form Handling
* Validation
* Error Management
* Performance
* Cleaner Code

---

# 3. What is Zod ?

Zod is a validation library.

It validates:

* String
* Email
* Password
* Number
* Array
* Object

using schema.

---

# 4. Why Use React Hook Form + Zod ?

Without libraries:

```txt id="u3m8vn"
useState
useState
useState
useState
manual validation
manual error handling
```

Becomes messy.

---

# 5. Modern Industry Stack

| Tool            | Work          |
| --------------- | ------------- |
| React Hook Form | Form Handling |
| Zod             | Validation    |
| Prisma          | Database ORM  |
| Next.js API     | Backend       |

---

# 6. Install Packages

```bash id="p7v1tx"
npm install react-hook-form zod @hookform/resolvers
```

---

# 7. Full Project Structure

```txt id="f2n9qw"
app/
├── register/
│   └── page.jsx
│
├── api/
│   └── register/
│       └── route.js
│
components/
└── RegisterForm.jsx
│
schemas/
└── registerSchema.js
│
lib/
└── prisma.js
```

---

# 8. Why `"use client"` ?

Forms need:

* Browser Event
* onSubmit
* Hooks
* Input State

So form components MUST use:

```js id="m4x2kr"
"use client";
```

---

# 9. Zod Schema

## schemas/registerSchema.js

```js id="n8v5qt"
import { z } from "zod";

export const registerSchema = z.object({

    name: z
        .string()
        .min(3, "Minimum 3 characters"),

    email: z
        .email("Invalid email"),

    password: z
        .string()
        .min(6, "Minimum 6 characters"),
});
```

---

# 10. Schema Concept

```txt id="r7p1na"
Form Data
    │
    ▼
Zod Schema
    │
    ▼
Validation Result
```

---

# 11. Full Register Form

## components/RegisterForm.jsx

```jsx id="w9m4cx"
"use client";

import { useForm } from "react-hook-form";

import { zodResolver }
from "@hookform/resolvers/zod";

import { registerSchema }
from "@/schemas/registerSchema";

export default function RegisterForm() {

    const {

        register,

        handleSubmit,

        formState: {
            errors,
            isSubmitting,
        },

        reset,

    } = useForm({

        resolver:
            zodResolver(registerSchema),
    });

    // SUBMIT FUNCTION
    async function onSubmit(data) {

        const response =
            await fetch("/api/register", {

                method: "POST",

                headers: {
                    "Content-Type":
                        "application/json",
                },

                body: JSON.stringify(data),
            });

        const result =
            await response.json();

        alert(result.message);

        reset();
    }

    return (

        <form onSubmit={handleSubmit(onSubmit)}>

            {/* NAME */}
            <div>

                <input
                    type="text"
                    placeholder="Name"

                    {...register("name")}
                />

                {
                    errors.name && (

                        <p>
                            {
                                errors.name.message
                            }
                        </p>
                    )
                }

            </div>

            {/* EMAIL */}
            <div>

                <input
                    type="email"
                    placeholder="Email"

                    {...register("email")}
                />

                {
                    errors.email && (

                        <p>
                            {
                                errors.email.message
                            }
                        </p>
                    )
                }

            </div>

            {/* PASSWORD */}
            <div>

                <input
                    type="password"
                    placeholder="Password"

                    {...register("password")}
                />

                {
                    errors.password && (

                        <p>
                            {
                                errors.password.message
                            }
                        </p>
                    )
                }

            </div>

            <button
                type="submit"

                disabled={isSubmitting}
            >

                {
                    isSubmitting
                        ? "Loading..."
                        : "Register"
                }

            </button>

        </form>
    );
}
```

---

# 12. Important React Hook Form Functions

| Function       | Work          |
| -------------- | ------------- |
| register()     | Connect Input |
| handleSubmit() | Handle Submit |
| errors         | Form Errors   |
| reset()        | Reset Form    |
| isSubmitting   | Loading State |

---

# 13. What is `register()` ?

```jsx id="y5v7qa"
{...register("email")}
```

Connects input to:

* React Hook Form
* Validation
* Form State

---

# 14. Error Handling

```jsx id="z2k8mc"
errors.email.message
```

Shows:

```txt id="x7t1pb"
Email validation error
```

---

# 15. Per Field Error Flow

```txt id="p4n6qw"
Input Field
      │
      ▼
register()
      │
      ▼
Zod Validation
      │
      ├── Invalid
      │      ▼
      │ Show Error
      │
      └── Valid
             ▼
          Continue
```

---

# 16. Why Use `handleSubmit()` ?

```jsx id="u8m4vr"
handleSubmit(onSubmit)
```

Automatically:

* Prevents Reload
* Validates Form
* Sends Data
* Handles Errors

---

# 17. Form Submit Flow

```txt id="w3k9tx"
User Fill Form
        │
        ▼
Click Submit
        │
        ▼
handleSubmit()
        │
        ▼
Zod Validation
        │
        ├── Error
        │      ▼
        │ Show Errors
        │
        └── Valid
               ▼
           onSubmit()
               │
               ▼
           API Request
```

---

# 18. API Route Validation

IMPORTANT:

```txt id="c1v7pb"
Always validate AGAIN on backend
```

---

# 19. API Route

## app/api/register/route.js

```js id="q8m3nc"
import { prisma } from "@/lib/prisma";

import { registerSchema }
from "@/schemas/registerSchema";

export async function POST(req) {

    const body = await req.json();

    // SAFE VALIDATION
    const validated =
        registerSchema.safeParse(body);

    // VALIDATION FAILED
    if (!validated.success) {

        return Response.json({

            message:
                "Validation Failed",

            errors:
                validated.error.flatten(),
        });
    }

    // SAVE DATABASE
    const user =
        await prisma.user.create({

            data: validated.data,
        });

    return Response.json({

        message:
            "Register Success",

        data: user,
    });
}
```

---

# 20. What is `safeParse()` ?

```js id="n2t8qw"
registerSchema.safeParse(data)
```

Checks validation safely.

---

# 21. safeParse Result

## VALID

```js id="y4m1pk"
{
    success: true,
    data: {...}
}
```

---

## INVALID

```js id="g8x5vb"
{
    success: false,
    error: {...}
}
```

---

# 22. Why Validate Again on Backend ?

Because frontend validation can be bypassed.

Example:

```txt id="s3w7qc"
Postman
Custom Script
Direct API Request
```

can skip frontend validation.

---

# 23. Form Error Architecture

```txt id="l6n9pa"
Input
   │
   ▼
React Hook Form
   │
   ▼
Zod Schema
   │
   ├── Invalid
   │      ▼
   │ errors object
   │
   └── Valid
          ▼
      Submit API
```

---

# 24. Loading State

```jsx id="v1k3ra"
isSubmitting
```

Automatically provided by:

React Hook Form

---

# 25. Disable Button During Submit

```jsx id="m5x7tw"
<button disabled={isSubmitting}>
```

---

# 26. Reset Form

```js id="r2n8qb"
reset();
```

Clears all form fields.

---

# 27. Password Confirm Validation

## Zod Example

```js id="t9p4vc"
import { z } from "zod";

export const schema = z
    .object({

        password:
            z.string(),

        confirmPassword:
            z.string(),
    })

    .refine(

        (data) =>
            data.password ===
            data.confirmPassword,

        {
            message:
                "Password not match",

            path: [
                "confirmPassword",
            ],
        }
    );
```

---

# 28. What is `.refine()` ?

Used for:

* Custom Validation
* Password Match
* Advanced Rules

---

# 29. Real World Form Architecture

```txt id="f8m1pw"
Input Fields
      │
      ▼
register()
      │
      ▼
React Hook Form
      │
      ▼
Zod Schema Validation
      │
      ▼
Error Handling
      │
      ▼
handleSubmit()
      │
      ▼
API Request
      │
      ▼
Backend Validation
      │
      ▼
Prisma ORM
      │
      ▼
Database
```

---

# 30. Most Important Best Practices

## RULE 1

```txt id="z5x2na"
Validate BOTH frontend and backend
```

---

## RULE 2

```txt id="y9v4qc"
Never trust frontend validation
```

---

## RULE 3

```txt id="u1m7pb"
Use schema validation for scalability
```

---

## RULE 4

```txt id="r4n8tx"
Show per-field errors
```

---

## RULE 5

```txt id="j8k3wa"
Disable submit button while loading
```

---

# 31. React Hook Form vs useState

| useState Form     | React Hook Form    |
| ----------------- | ------------------ |
| More Re-render    | Better Performance |
| Manual Validation | Easy Validation    |
| More Code         | Cleaner Code       |
| Manual Errors     | Automatic Errors   |
| Hard Large Form   | Easy Large Form    |

---

# 32. Most Important Interview Questions

## Q1: Why use React Hook Form ?

Better performance and cleaner forms.

---

## Q2: Why use Zod ?

Schema-based validation.

---

## Q3: Why validate on backend again ?

Frontend validation can be bypassed.

---

## Q4: What is `register()` ?

Connects input to form state.

---

## Q5: What is `handleSubmit()` ?

Handles validation + form submit.

---

## Q6: What is `safeParse()` ?

Safe validation check using Zod.

---

## Q7: Why use `.refine()` ?

Custom validation logic.

---

# 33. Final Golden Rule

```txt id="x6t1mq"
React Hook Form
=
Form Management

Zod
=
Validation

Prisma
=
Database ORM
```

---

# 34. Final Quick Summary

| Concept        | Work              |
| -------------- | ----------------- |
| useForm()      | Create Form       |
| register()     | Connect Input     |
| handleSubmit() | Handle Submit     |
| errors         | Validation Errors |
| z.object()     | Create Schema     |
| safeParse()    | Validate Data     |
| refine()       | Custom Validation |
| isSubmitting   | Loading State     |
| reset()        | Clear Form        |
| Prisma         | Save Database     |
